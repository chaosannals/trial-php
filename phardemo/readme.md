# phar

```bash
# composer autoload
# --no-scripts 跳过定义 composer.json scripts 下的 文件中脚本的执行。
# --no-dev 禁止 autoload-dev 规则。
# --optimize(-o) 优化
composer dump-autoload --no-scripts --no-dev -o

# 打包
php build.php
```
